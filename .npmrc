# INFOMIND UX Guide System — npm 설정
#
# legacy-peer-deps: peer dependency 충돌 자동 우회 (Eleventy 3.x + 일부 플러그인 호환)
# 이 줄이 있으면 `npm install --legacy-peer-deps` 플래그 없이도 동일하게 동작한다.
legacy-peer-deps=true

# 정확한 버전 잠금 — 보안/재현성
save-exact=false
